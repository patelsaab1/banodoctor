# Bano Doctor

Laravel 10 site for medical college counselling (MBBS, MD/MS, MDS).

## Setup

```bash
composer install
cp .env.example .env
php artisan key:generate
```

Set `APP_URL` to `https://www.banodoctor.com` on production. Do not commit `.env`.
